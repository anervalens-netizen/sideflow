# SideFlow minimal delivery — 25 September 2026

## Accepted installed build

The owner-installed version is **0.2.1 / code10**, functional source **`58990552db4fbf404e54d76f80859a15d443a81e`**, in PR #9. The subsequent documentation commit changes no Android source or build settings. The owner reported a close flicker after code8; code10 targets that teardown path, was installed in place, and the owner responded “ok” and requested closure. Opening geometry and the two-column product scope remain unchanged.

Actual observations: same certificate on baseline and update; install-r successful; four sections and18 targets retained; overlay and Accessibility retained; foreground runtime running. Physical swipe opened the panel. Outside-close returned to handle-only state. A 350ms window-state sample proves the end state, not a high-speed visual/frame-time measurement. Attempts at phone screen recording failed with permission errors and are not passing evidence.

On the final functional source, tests, debug build and lint passed locally; exact-head GitHub build/lint and API35 instrumentation are green; Codex review on5899055 found no further issues. Lint remains **0 errors /39 debug warnings**. Prior API26/API35 and rollback rehearsals apply to their recorded earlier source, not an unperformed rerun of every case after the fix.

## Delivered versus deferred

Tracker #8 covers the delivered minimal implementation and installation. Its unfinished broad qualification is explicitly transferred to **#10**, not marked passed. This includes a same-certificate **signed R8 release**, a new recovery APK compatible with code10, all real app/WebAPK launches, remaining OxygenOS/geometry/lifecycle tests and measured memory/frame/battery comparisons. Current phone APK is the debug variant. The old recoverycode9 is historical and cannot normally downgrade code10.

No new UI settings, tools, themes or notification tracking are in scope. Private configuration, package inventory, recordings and signing files remain outside the public repository. No uninstall/reset is authorized for recovery.

## Evidence and continuation

Private Server evidence: `/home/andrei/outputs/sideflow-minimal-20260925/phone-install-receipt.md`, `close-flicker-fix-receipt.md`, `close-flicker-fix-gates.log` and `phone-preinstall/`. Source history retains073ba4b and the private recovery worktree1689dad. Historical P0-P4 expansion documents are provenance only and do not reintroduce deleted capabilities or retired AI PR Review.

After PR #9 merges, main is the development entrypoint; preserve the original baseline and private recovery worktree. Follow-up #10 and current owner requests, not obsolete handoff instructions, determine the next work. Merge SHA/checks and closure receipts are recorded on #8 and ContextKeep.
